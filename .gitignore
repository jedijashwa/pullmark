.build/
dist/
.DS_Store
*.xcodeproj
.swiftpm/
# Claude Code: agent and skill definitions are shared project tooling;
# everything else in .claude/ (worktrees, local settings) stays local.
.claude/*
!.claude/agents/
!.claude/skills/
CLAUDE.local.md
